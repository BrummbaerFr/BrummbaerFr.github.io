function syracuseCipher() {
  var plain = document.getElementById("plain").value.toUpperCase();
  alert(plain);
  var plainArray = plain.split("");
  alert(plainArray);
    for (var i = 0; i < plainArray.length; i++) {
      if (plainArray[i] = " ") {
        plainArray.split(i,1);
      }

    }
}
